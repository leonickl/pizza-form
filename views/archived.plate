{{ == head("Archiv der Bestellungen (".$orders->count().")") }}

{{ if: $unarchived }}
    <div class="notification"><p class="m-0 p-0">Bestellung von <b>{{ $unarchived->name }}</b> entarchiviert.</p></div>
{{ if; }}

<div class="desktop-only overflow-x-auto">
    <table class="styled w-full w-min-40">
        <thead>
            <tr>
                <th>Bezahlt</th>
                <th>ID</th>
                <th>Name</th>
                <th>E-Mail</th>
                <th>Tage</th>
                <th>Typ</th>
                <th>Extra</th>
                <th>Erstellt/Geändert</th>
                <th>Archiviert</th>
                <th></th>
            </tr>
        </thead>

        <tbody>
            {{ each: $orders->reverse() as $order }}
                <tr>
                    <td>
                        <button class="checkbox-readonly" title="Status wechseln"
                            style="background-color: {{ $order->paid ? 'lightgreen' : 'red' }}"
                            readonly></button>
                    </td>
                    <td>{{ $order->id }}</td>
                    <td class="text-wrap">{{ $order->name }}</td>
                    <td class="text-wrap">{{ $order->email }}</td>
                    <td>{{ $order->daysLabel() }}</td>
                    <td>{{ $order->type }}</td>
                    <td class="text-wrap">{{ $order->extra }}</td>
                    <td class="text-wrap">
                        {{ $order->created_at }}

                        {{ if: $order->created_at !== $order->modified_at }}
                            / {{ $order->modified_at }}
                        {{ if; }}
                    </td>
                    <td>{{ $order->archived_at }}</td>
                    <td>
                        <form action="{{ route('unarchive', id: $order->id) }}" method="post">
                            <button class="btn">Entarchivieren</button>
                        </form>
                    </td>
                </tr>
            {{ each; }}
        </tbody>
    </table>
</div>

<div class="mobile-only column mt">
    {{ each: $orders->reverse() as $order }}
        <div class="card">
            <div class="mb-05"><strong>ID:</strong> {{ $order->id }}</div>
            <div class="mb-05 text-wrap"><strong>Name:</strong> {{ $order->name }}</div>
            <div class="mb-05 text-wrap"><strong>E-Mail:</strong> {{ $order->email }}</div>

            {{ if: trim($order->daysLabel()) !== '' }}
                <div class="mb-05 text-wrap"><strong>Tage:</strong> {{ $order->daysLabel() }}</div>
            {{ if; }}

            <div class="mb-05 text-wrap"><strong>Typ:</strong> {{ $order->type }}</div>

            {{ if: trim($order->extra) !== '' }}
                <div class="mb-05 text-wrap"><strong>Extra:</strong> {{ $order->extra }}</div>
            {{ if; }}

            <div class="mb-05 text-wrap"><strong>Erstellt/Geändert:</strong>
                {{ $order->created_at }}

                {{ if: $order->created_at !== $order->modified_at }}
                    / {{ $order->modified_at }}
                {{ if; }}
            </div>

            <div class="mb-05 text-wrap"><strong>Archiviert:</strong>
                {{ $order->archived_at }}
            </div>

            <div class="row between items-center mt">
                <button class="checkbox-readonly" title="Status wechseln"
                    style="background-color: {{ $order->paid ? 'lightgreen' : 'red' }}"
                    readonly></button>
                <form action="{{ route('unarchive', id: $order->id) }}" method="post">
                    <button class="btn">Entarchivieren</button>
                </form>
            </div>
        </div>
    {{ each; }}
</div>
